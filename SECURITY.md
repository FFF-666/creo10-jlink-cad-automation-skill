# 安全与公开发布

提交前必须确认：

- 没有 `.prt`、`.asm`、STL、JAR、EXE、DLL、Creo/JDK 安装文件或 PTC 库。
- 没有课程原始 DOCX/PPTX、带签名的下载 URL、Cookie、令牌、密码或个人凭据。
- 公开文档使用 `<PROJECT_ROOT>`、`<OUTPUT_DIR>` 等占位符，不记录用户主目录。
- 任何模型哈希只作为验证证据，不把哈希当作模型本体。
- GUI 自动化必须是用户明确授权后的最后备用路线。

发现敏感内容时不要提交；先从工作树移除并轮换可能泄露的凭据。

